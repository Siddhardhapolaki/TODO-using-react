import { ToastState } from './ToastContext';
interface ToastProviderType {
    children: React.ReactNode;
}
export declare const initialState: ToastState;
export interface ToastOptions {
    position?: 'top-right' | 'top-left' | 'bottom-left' | 'bottom-right' | 'center' | 'top-center' | 'bottom-center' | 'left-center' | 'right-center';
    timer?: number;
}
declare const ToastContextProvider: React.FC<ToastProviderType>;
export default ToastContextProvider;
